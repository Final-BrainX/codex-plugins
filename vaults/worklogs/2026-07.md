# 2026-07 Worklog

## 2026-07-03 18:00 - Agent 문서화 체계 추가

완료: repo-local agent guide와 `vaults/` 라우팅 문서를 추가하고, BrainX MCP OAuth 전환 결정을 decision note로 기록했습니다.
아티팩트: `AGENTS.md`, `vaults/INDEX.md`, `vaults/agents/plugin.md`, `vaults/workflows/verification.md`, `vaults/workflows/worklog.md`, `vaults/decisions/2026-07-03-brainx-oauth-mcp.md`
검증: 핵심 경로 존재 확인, vault routing 검색, `git diff --check` 통과.

## 2026-07-03 17:59 - BrainX context source 문서화

완료: BrainX plugin을 단순 노트 도구가 아니라 프로젝트 메모리/사용자 지식 컨텍스트 소스로 유지하도록 agent guide, vault index, plugin guide, durable improvement를 보강했습니다.
아티팩트: `AGENTS.md`, `vaults/INDEX.md`, `vaults/agents/plugin.md`, `vaults/durable/INDEX.md`
검증: JSON parse, skill frontmatter 확인, vault routing 검색 통과.

## 2026-07-08 14:20 - 관련 프로젝트 경계 문서화

완료: `../brainX`와 `../brainx-cli`를 참조 프로젝트로 기록하고, 이 저장소 내부에서만 문서와 코드를 변경한다는 범위를 README와 agent 문서에 반영했습니다.
아티팩트: `README.md`, `AGENTS.md`, `vaults/INDEX.md`, `vaults/agents/plugin.md`, `vaults/worklogs/2026-07.md`
검증: `rg --files`, 관련 경로/표기 검색, JSON parse check, `git diff --check`를 실행했습니다. 잘못된 CLI 경로 오타는 발견되지 않았고, `git diff --check`는 기존 line-ending 변환 warning만 표시했습니다.
